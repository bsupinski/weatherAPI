import { view } from "./view.js";

class HourView extends view {
  _parentElement = document.querySelector("main");

  // _markup() {
  //   this._data.hourly.map((hour) => {});
  // }
}

export default new HourView();
